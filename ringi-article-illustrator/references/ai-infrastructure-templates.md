# Ringi AI Infrastructure 专属场景模板库

1. **GPU 算力集群与任务调度**（`任务分发` -> `GPU 算力池` -> `并行训练` -> `集群监控`）
2. **海量数据吞吐与 RDMA 直通**（`数据源` -> `分布式清洗` -> `NVMe 缓存` -> `显存直通`）
3. **分布式训练与故障自愈**（`节点故障` -> `快照恢复` -> `热插拔自动替换`）
4. **大模型推理网关与 KV Cache**（`并发请求` -> `动态批处理` -> `KV Cache 显存池` -> `流式输出`）
5. **C++ 数据面与 Python 胶水层**（`Python API` -> `PyBind11 高速桥` -> `ATen/CUDA 算子熔炉`）
6. **张量物理内存模型与 Strides**（`高维张量` -> `Strides 步长投影` -> `1D 连续物理存储`）
7. **RAII 显存保险库与 std::move 零拷贝**（`入作用域自动抓取` -> `所有权钥匙移交` -> `出作用域安全回收`）
